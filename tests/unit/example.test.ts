import { describe, it, expect } from 'vitest';

describe('Example test', () => {
	it('should pass', () => {
		expect(true).toBe(true);
	});

	it('can do basic math', () => {
		expect(1 + 1).toBe(2);
	});
});
